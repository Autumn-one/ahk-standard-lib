/**
 * Pass in several parameters, print them and wrap them, separated by Spaces
 * @param params* 
 */
println(params*) => void

/**
 * Pass in several arguments, print them, separated by Spaces, and the function does not wrap lines
 * @param params 
 */
print(params*) => void